# VibeCatchEditor AI 开发约定

## 任务入口

本目录是独立 Windows Catch 编辑器项目。开始工作先阅读：

1. [产品定义](docs/PRODUCT.md)。
2. [技术架构](docs/ARCHITECTURE.md)。
3. [当前实现任务](docs/IMPLEMENTATION_PLAN.md)。
4. 涉及界面或数据时，阅读对应的 [界面规范](docs/EDITOR_UI.md) 与 [模型规范](docs/PROJECT_MODEL.md)。
5. 涉及用户可见文案时，阅读 [本地化维护](docs/LOCALIZATION.md)。

用户最新要求优先。文档中的“默认方案”可随实际证据调整；不要把建议描述为用户已确认的硬性要求。完成后只报告最终保留的修改、实际执行的验证和剩余限制。

## 开发边界

- 当前优先交付 M1 编辑器界面，不自动扩展到完整客户端、插件系统、曲库或在线服务。
- 首个可用版本包括 M1 + M2：真实 Catch beatmap 读写、4/6 分拍 fruit 编辑、手绘贝塞尔到 slider/fruit stream、真实音乐播放暂停及进度条拖动。不得将这些能力降为可选项或用演示替代。
- 使用 C# / .NET 8，Win32 窗口与输入，自绘客户端。首版默认 DX11 + Direct2D/DirectWrite；DX12、Vulkan 是允许的后续选择，不要求同时实现。
- “开发 API”暂解释为桌面应用的 C# 技术栈，不据此创建 HTTP 服务。
- 不引入 Unity、lazer 编辑器宿主或对运行中 osu! 进程的依赖。
- 第三方绑定只解决系统 API 接入；主节点、交互和工程格式由本项目拥有。
- 不提前搭建通用 GUI 框架、渲染图、多后端管理或插件架构。仅为已实现功能建立边界。
- 自有 GUI、菜单、状态与诊断文案从语言表读取，不硬编码在 C# 中。以 `en.json` 为主表，各语言一张同键 JSON；新增或修改文案同步维护中文和英文，校验键与占位符。用户数据、文件内容及已有名称不随界面语言改写。

## 数据与兼容性

- 自有工程保存创作意图；`.osu`、生成路径、RNG 偏移和预览对象属于派生结果。
- `.osu` 解析和写出自行实现，以 [stable 文件规范](docs/STABLE_FORMAT.md) 为准；首版输出 v14 / Mode=2。lazer 的角色是功能与算法参考。
- 时间轴 Y 是时间，slider 几何 Y 是路径坐标。类型、变量和 UI 标注必须区分。
- 辅助节点不自动生成 tick；香蕉预览不提供逐根位置编辑。
- 4/6 分拍指每拍 4/6 等分，不是 4/4 或 6/8 拍号；编辑器吸附不隐式修改 SliderTickRate。
- 保存贝塞尔控制柄与时间—X 主节点；不能只保留采样折线。读取与输出 `.osu`、自有工程读写是不同操作，均需落实。
- 真实音乐驱动统一编辑时钟；播放、暂停、seek 必须作用于音频，不能只移动 UI 播放头。
- 演示、目标轨迹、已生成结果、已验证结果必须清楚区分。没有对照证据时，不宣称 stable 精确一致。
- 后续 RNG 计算使用完整谱面上下文；视口裁剪仅影响绘制，不改变转换输入。
- 输入、渲染与领域模型分离。核心模型不得引用 HWND、COM、Direct2D 或 Vortice 类型。

## 实施与验证

- 先检查实际目录与 SDK，再编辑。不要为了文档任务安装依赖、生成空项目或初始化远端仓库。
- 实现时固定 SDK、语言版本和包版本；核对目标框架及传递依赖，不使用浮动最新版。
- 注释只说明实现边界上不明显的理由，过程记录留在会话。
- 对坐标变换、吸附、撤销等有行为风险的逻辑做小而有效的测试；视觉与窗口行为通过实际运行验证。
- 自动音频回归默认将输出 PCM 静音，保留真实设备时钟和静音前采样比较，不修改系统音量。需要耳听的验证另行说明，避免自动测试反复发出测试音。
- 明确区分“编译通过”“窗口已运行”“交互已验证”。不能运行时报告缺口，不以静态检查替代运行结论。
- 引入或改编外部源码时记录仓库、commit、文件及许可证，保留需要的声明。
- 相关功能先参考 [lazer 代码入口](docs/LAZER_REFERENCE.md)。核对上游算法与边界，按本项目自绘和数据模型适配；不直接把依赖 osu!framework 的 UI 类当作 Win32 控件。
- 构建产物、日志和截图放在项目内约定的产物目录；不改动其他项目。
- 不自动启动子代理；仅在用户明确要求并行代理工作时使用。
